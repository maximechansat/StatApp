# YOLO Scaling Law Study Configuration

# Dataset configuration
dataset:
  root_dir: "/home/onyxia/work/data/solar_panel_dataset"
  yaml_file: "solar_panel_dataset.yaml"

# Study parameters
study:
  seeds: [4221376603, 3810243382, 6937632396]
  dataset_fractions: [0.1, 0.25, 0.5, 1.0]
  model_variants: ["yolo11n.pt", "yolo11s.pt", "yolo11m.pt", "yolo11l.pt", "yolo11x.pt"]
  resolutions: [416, 640, 1280]
  epochs: 50

# Training parameters
training:
  patience: 0  # Early stopping patience
  lr0: 0.002  # Initial learning rate
  lrf: 0.002  # Final learning rate
  momentum: 0.937  # SGD momentum
  weight_decay: 0.0005  # Weight decay
  warmup_epochs: 3  # Warmup epochs
  warmup_momentum: 0.8  # Warmup momentum
  warmup_bias_lr: 0.1  # Warmup bias learning rate

# Results configuration
results:
  output_dir: "scaling_results"
  save_models: false
  save_plots: true
